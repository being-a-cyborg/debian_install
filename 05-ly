# donwload ly
git clone https://codeberg.org/fairyglade/ly.git /home/$USER/.local/distr/ly

# download zig
wget -P /home/$USER/.local/distr https://ziglang.org/download/0.15.1/zig-x86_64-linux-0.15.1.tar.xz

# untar zig
# build
# zig build installexe -Dinit_system=systemd
# enable
# systemctl enable ly.service
# systemctl disable getty@tty2.service

sudo mkdir /usr/share/xsessions/
sudo ln -sf /home/$USER/.local/distr/suckless/dwm.desktop /usr/share/xsessions/
sudo ln -sf /home/$USER/.local/distr/suckless/startdwm.sh /usr/local/bin/
